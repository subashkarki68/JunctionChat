{
  "dependencies": {
    "@elysiajs/jwt": "^1.1.1"
  },
  "devDependencies": {
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3"
  },
  "scripts": {
    "prepare": "husky",
    "pretty": "cd ./server && bun run pretty && cd ../client && bun run pretty"
  },
  "lint-staged": {
    "*.js": "prettier --write",
    "*.ts": "prettier --write",
    "*.css": "prettier --write",
    "*.json": "prettier --write"
  }
}
